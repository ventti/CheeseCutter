---
purpose: "Orientation map — READ FIRST to locate a module or trace what depends on what, before grep/find. (Point lookups: just grep.)"
generated_by: tools/genmap.py   # do NOT edit this file; edits are overwritten
how_to_use: "Each row = module, one-line synopsis, first-party dependency edges. module a.b.c lives at src/a/b/c.d; open a file only after the map points you at it."
how_to_update: "Edit the module header's one-line synopsis (first sentence after the license line), then run `make map` (also in `make docs` / `mise run build`). New modules + changed imports are picked up automatically; `make check-map` fails if stale."
see_also: "doc/adr/ — append-only decision records: the WHY this file cannot show."
---

# Architecture map

<!-- GENERATED by tools/genmap.py — do NOT hand-edit. Run `make map` (or `make docs`) to regenerate. -->

Auto-derived overview of the 40 first-party D modules under `src/` (vendored `src/derelict/**` and the C/C++ ReSID sources are omitted). Module `a.b.c` lives at `src/a/b/c.d`. *Depends on* lists only imports of other first-party modules — the internal dependency edges.

The dependency graph and module list are generated from the source; the only hand-written part is each module's one-line **synopsis**, taken from its header comment. Keep that line current when a module's role changes (see CLAUDE.md).

## Packages

| Package | Role |
| --- | --- |
| `(root)` | Program entry points (editor + ct2util CLI) and generated man-page tables. |
| `audio` | Audio engine: playback driver, ReSID bridge, visualizer, and remote (VICE / Ultimate) backends. |
| `audio.resid` | D-side glue to the ReSID SID emulation core. |
| `com` | Common foundation: framebuffer, keyboard, session/config, 6502 CPU, the shortcut registry and shared utilities. |
| `ct` | CheeseTracker song model: in-memory format (base), build/export, dump, and purge/optimize. |
| `seq` | Sequencer: the pattern / track data tables and follow-play that turn the song model into playback. |
| `ui` | SDL2 editor UI: top-level layout, windows, dialogs, menu bar, command palette, status bar and help. |

## Modules

### `(root)`

| Module | Synopsis | Depends on |
| --- | --- | --- |
| `ct2util` | ct2util CLI — compiles/optimizes a .ct song into .prg or .sid, sharing ct.build/ct.purge with the editor. | `com.cpu`, `com.util`, `ct.base`, `ct.build`, `ct.dump`, `ct.purge` |
| `main` | Editor entry point — parses the CLI, initialises SDL/audio/framebuffer and the UI, then runs the main loop. | `audio.audio`, `audio.callback`, `audio.player`, `audio.remote`, `audio.resid.filter`, `audio.timer`, `audio.ultimate`, `audio.vice`, `com.fb`, `com.kbd`, `com.session`, `com.util`, `ct.base`, `manpage`, `seq.sequencer`, `ui.input`, `ui.shorthelp`, `ui.ui` |
| `manpage` | Man-page generator. | `com.util` |

### `audio`

| Module | Synopsis | Depends on |
| --- | --- | --- |
| `audio` | SDL audio device setup and the mix buffer that carries reSID output to the sound card. | `audio.callback`, `audio.player`, `audio.remote`, `audio.resid.filter` |
| `callback` | Realtime audio callback — advances the player each tick, runs the 6502 player code, and captures SID register dumps (SongState). | `audio.audio`, `audio.player`, `audio.timer`, `audio.visualizer`, `com.cpu`, `com.session`, `ct.base` |
| `player` | High-level playback control: play/stop/keyjam state, SID model selection and reSID engine wiring. | `audio.audio`, `audio.callback`, `audio.remote`, `audio.resid.filter`, `audio.timer`, `audio.visualizer`, `com.cpu`, `com.session`, `ct.base`, `seq.sequencer`, `ui.ui` |
| `remote` | Transport-agnostic core for "remote" playback backends — playing the live song image on real or emulated C64 hardware instead of (only)… | `ct.base`, `ct.build` |
| `timer` | Playback timing: the frame- and row-tick counters that pace the player. | `audio.audio`, `audio.callback`, `com.session`, `ct.base` |
| `ultimate` | C64 Ultimate (1541U / Ultimate64) playback transport. | `audio.remote` |
| `vice` | VICE (x64sc) playback transport over the binary monitor. | `audio.remote` |
| `visualizer` | Oscilloscope / SID-register visualizer rendering into the text framebuffer. | — |

### `audio.resid`

| Module | Synopsis | Depends on |
| --- | --- | --- |
| `filter` | Filter-parameter tables bridging D to the ReSID 6581/8580 filter curves. | — |

### `com`

| Module | Synopsis | Depends on |
| --- | --- | --- |
| `cpu` | Cycle-stepped MOS 6510 CPU emulator that runs the C64 player code in-process. | — |
| `fb` | Text-mode framebuffer — Video/Screen abstraction, character-cell rendering and the Visualizer interface. | `audio.audio`, `com.util` |
| `kbd` | Keyboard scancode translation, including the super/cmd layer, into editor key events. | `ui.input` |
| `selection` | Generic 2-D selection geometry + a block clipboard, shared by the sequencer note and track columns. | — |
| `session` | Editor session state — the undo/redo stack (Undoable) plus editor and tracklist state. | `com.fb`, `com.util`, `ct.base`, `seq.sequencer`, `ui.ui` |
| `shortcuts` | Configurable keyboard shortcut binding system. | `ui.input` |
| `util` | Shared utilities and app identity — APP_NAME/APP_VERSION (string-imported from `Version`) and PET-string helpers. | — |

### `ct`

| Module | Synopsis | Depends on |
| --- | --- | --- |
| `base` | In-memory song model — the .ct/.ct2 data format: notes, commands, instruments, tables and memory offsets. | `com.cpu`, `com.util`, `ct.purge` |
| `build` | Song-image build and export — validation plus the FullPrg / OptimizedPrg / PSID formats (ExportOptions). | `com.cpu`, `com.util`, `ct.base`, `ct.dump`, `ct.purge` |
| `dump` | Serializes the song model to .ct data (dumpData). | `com.util`, `ct.base` |
| `purge` | Optimize/clean pass — removes unused instruments, tables and sequence rows (Purge). | `com.util`, `ct.base` |

### `seq`

| Module | Synopsis | Depends on |
| --- | --- | --- |
| `fplay` | Follow-play window that keeps the editor view tracking the playing position. | `audio.audio`, `audio.player`, `audio.timer`, `com.fb`, `com.session`, `ct.base`, `seq.seqtable`, `seq.sequencer`, `ui.input`, `ui.ui` |
| `seqtable` | Per-voice note-sequence editor tables (SeqVoice / SequenceTable). | `audio.player`, `audio.visualizer`, `com.fb`, `com.selection`, `com.session`, `com.util`, `ct.base`, `main`, `seq.sequencer`, `ui.input`, `ui.ui` |
| `sequencer` | Central sequencer view — hosts the per-voice track and sequence editor tables and the orderlist. | `audio.audio`, `audio.player`, `com.fb`, `com.selection`, `com.session`, `com.shortcuts`, `com.util`, `ct.base`, `main`, `seq.fplay`, `seq.seqtable`, `seq.trackmap`, `seq.tracktable`, `ui.dialogs`, `ui.help`, `ui.input`, `ui.ui` |
| `trackmap` | Orderlist 'trackmap' editor tables mapping tracks to sequences (TrackmapVoice / TrackmapTable). | `com.fb`, `com.session`, `ct.base`, `main`, `seq.sequencer`, `seq.tracktable`, `ui.input`, `ui.ui` |
| `tracktable` | Track editor tables (TrackVoice / BaseTrackTable / TrackTable) layered over the sequence tables. | `com.fb`, `com.selection`, `com.session`, `com.util`, `ct.base`, `main`, `seq.seqtable`, `seq.sequencer`, `ui.dialogs`, `ui.input`, `ui.ui` |

### `ui`

| Module | Synopsis | Depends on |
| --- | --- | --- |
| `dialogs` | Modal dialogs — query / confirm / string-entry and the help viewer (QueryDialog family). | `com.fb`, `com.session`, `com.util`, `ct.base`, `ct.build`, `main`, `ui.help`, `ui.input`, `ui.ui` |
| `help` | ContextHelp data type backing the F12 per-context help pages. | `com.util` |
| `input` | Editor input primitives — Cursor, Keyinfo and the value-changed handler mixin for editable fields. | `audio.player`, `com.fb`, `com.session`, `com.util`, `ct.base`, `seq.sequencer`, `ui.ui` |
| `keymap` | Registers all editor shortcuts and context bindings into com.shortcuts and applies derived menu labels. | `audio.audio`, `audio.callback`, `audio.player`, `audio.timer`, `com.fb`, `com.session`, `com.shortcuts`, `com.util`, `ct.base`, `ct.build`, `ct.purge`, `main`, `seq.fplay`, `seq.sequencer`, `ui.dialogs`, `ui.help`, `ui.input`, `ui.menubar`, `ui.shorthelp`, `ui.statusbar`, `ui.tables`, `ui.toplevel`, `ui.ui`, `ui.window` |
| `menubar` | Top-bar dropdown menu system. | `com.session`, `com.shortcuts`, `com.util`, `ui.input`, `ui.ui` |
| `palette` | Command palette: a VS-Code-style "> " prompt that searches every command reachable from the current focus plus the .ct/.ct2 songs in the… | `com.session`, `com.shortcuts`, `ui.input`, `ui.menubar`, `ui.ui` |
| `shorthelp` | Generates user-facing help (the F12 ContextHelp pages) and a Markdown keyboard reference from the single shortcut registry in com.shortcuts. | `com.shortcuts`, `ui.help` |
| `statusbar` | Top info bar and bottom status line (Infobar / Statusline). | `audio.audio`, `audio.callback`, `audio.player`, `audio.timer`, `com.fb`, `com.session`, `com.shortcuts`, `com.util`, `ct.base`, `main`, `seq.sequencer`, `ui.help`, `ui.input`, `ui.ui`, `ui.window` |
| `tables` | Instrument and command editor panels (HexTable / InsTable / CmdTable). | `audio.player`, `audio.visualizer`, `com.fb`, `com.session`, `com.shortcuts`, `com.util`, `ct.base`, `ct.purge`, `seq.sequencer`, `ui.dialogs`, `ui.help`, `ui.input`, `ui.ui` |
| `toplevel` | Top-level editor screen composing the sequencer, editor tables and bars (Toplevel). | `audio.audio`, `audio.callback`, `audio.player`, `audio.timer`, `com.fb`, `com.session`, `com.shortcuts`, `com.util`, `ct.base`, `ct.build`, `ct.purge`, `main`, `seq.fplay`, `seq.sequencer`, `ui.help`, `ui.input`, `ui.statusbar`, `ui.tables`, `ui.ui`, `ui.window` |
| `ui` | UI facade — owns the main UI object and visualizer mode, and re-exports the window/bar modules. | `audio.audio`, `audio.callback`, `audio.player`, `audio.remote`, `audio.timer`, `com.fb`, `com.session`, `com.shortcuts`, `com.util`, `ct.base`, `ct.build`, `ct.purge`, `main`, `seq.fplay`, `seq.sequencer`, `ui.dialogs`, `ui.help`, `ui.input`, `ui.keymap`, `ui.menubar`, `ui.palette`, `ui.shorthelp`, `ui.statusbar`, `ui.tables`, `ui.toplevel`, `ui.window` |
| `window` | Base windowing primitives — Window, WindowSwitcher, Rectangle, Hotspot. | `com.fb`, `com.session`, `com.shortcuts`, `com.util`, `ct.base`, `main`, `ui.help`, `ui.input` |
